var RotatingColorSolver = new Class({
  current: 5,
  
  move : function() {    
    if (this.current == 5)
      this.current = 0;
    else
      this.current++
      
    return this.current;
  }
});

solvers.set("rotating colors", RotatingColorSolver);